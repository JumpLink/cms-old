.modal(tabindex='-1', role='dialog')
  .modal-dialog
      .modal-content
        .modal-header(ng-show='user.name')
          button.close(type='button', ng-click='$hide()') ×
          h4.modal-title
            | User - {{user.name}}
        .modal-body
          | content
        .modal-footer
          button.btn.btn-default(type='button', ng-click='$hide()') Close
